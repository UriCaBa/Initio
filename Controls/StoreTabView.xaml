<UserControl x:Class="NewPCSetupWPF.Controls.StoreTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Padding="12,8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <ItemsControl x:Name="CategoryTabsPanel"
                          AutomationProperties.AutomationId="CategoryTabsPanel"
                          Grid.Row="0"
                          ItemsSource="{Binding Store.Categories}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Content="{Binding DisplayName}"
                                Command="{Binding DataContext.SelectStoreCategoryCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                CommandParameter="{Binding}">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource PillButtonStyle}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                            <Setter Property="Background" Value="{DynamicResource AccentBrush}" />
                                            <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <Grid Grid.Row="1" Margin="0,8,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="280" />
                </Grid.ColumnDefinitions>
                <TextBox AutomationProperties.AutomationId="StoreQueryTextBox" Text="{Binding Store.Query, UpdateSourceTrigger=PropertyChanged}" />
            </Grid>

            <ListView x:Name="StoreListView"
                      AutomationProperties.AutomationId="StoreListView"
                      Grid.Row="2"
                      Margin="0,8,0,0"
                      ItemsSource="{Binding Store.VisibleItems}"
                      SelectedItem="{Binding Store.SelectedItem, Mode=TwoWay}">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Width="42" Header="Sel">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <CheckBox HorizontalAlignment="Center"
                                              Style="{StaticResource GlobalCheckBoxStyle}"
                                              IsChecked="{Binding IsSelected, Mode=TwoWay}" />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Width="180" Header="Name" DisplayMemberBinding="{Binding Name}" />
                        <GridViewColumn Width="120" Header="Category" DisplayMemberBinding="{Binding Category}" />
                        <GridViewColumn Width="220" Header="Winget ID" DisplayMemberBinding="{Binding WingetId}" />
                        <GridViewColumn Width="60" Header="Score" DisplayMemberBinding="{Binding TrendScore}" />
                        <GridViewColumn Width="120" Header="Status">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <Border Background="{DynamicResource SurfaceHighBrush}" CornerRadius="6" Padding="6,2">
                                        <TextBlock Text="{Binding CatalogStatus}" FontSize="11" FontWeight="SemiBold">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="{DynamicResource TextMutedBrush}" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding CatalogStatus}" Value="Installed">
                                                            <Setter Property="Foreground" Value="{DynamicResource SuccessBrush}" />
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding CatalogStatus}" Value="In My Setup">
                                                            <Setter Property="Foreground" Value="{DynamicResource AccentBrush}" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </Border>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>

            <Grid Grid.Row="3" Margin="0,8,0,4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Button Content="Select All"
                        AutomationProperties.AutomationId="StoreSelectAllBtn"
                        Style="{StaticResource GhostButtonStyle}"
                        Command="{Binding StoreSelectAllCommand}"
                        Margin="0,0,5,0" />
                <Button Grid.Column="1"
                        Content="Select None"
                        AutomationProperties.AutomationId="StoreSelectNoneBtn"
                        Style="{StaticResource GhostButtonStyle}"
                        Command="{Binding StoreSelectNoneCommand}"
                        Margin="0,0,5,0" />
                <Border Grid.Column="2"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Center"
                        Background="{DynamicResource SurfaceHighBrush}"
                        CornerRadius="8"
                        Padding="10,4"
                        Margin="8,0,0,0">
                    <TextBlock Text="{Binding StoreSelectionSummary}" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="11" />
                </Border>
                <Button Grid.Column="3"
                        Content="Add to My Setup"
                        AutomationProperties.AutomationId="StoreAddToSetupBtn"
                        Command="{Binding AddStoreTrendAppCommand}" />
            </Grid>
        </Grid>
    </Border>
</UserControl>


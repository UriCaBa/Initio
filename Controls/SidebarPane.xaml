<UserControl x:Class="NewPCSetupWPF.Controls.SidebarPane"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="0"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled">
            <StackPanel Margin="14,14,14,0">
                <TextBlock Style="{StaticResource SidebarLabelStyle}" Text="THEME" />
                <ComboBox ItemsSource="{Binding ThemeOptions}"
                          SelectedItem="{Binding SelectedTheme, Mode=TwoWay}"
                          Margin="0,0,0,16"
                          AutomationProperties.AutomationId="ThemeComboBox">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding DisplayName}" />
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>

                <TextBlock Style="{StaticResource SidebarLabelStyle}" Text="QUICK PROFILES" />
                <ItemsControl ItemsSource="{Binding SelectionProfiles}" Margin="0,0,0,16">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Content="{Binding DisplayName}"
                                    AutomationProperties.AutomationId="{Binding Key, StringFormat=Profile{0}}"
                                    Command="{Binding DataContext.SelectProfileCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    CommandParameter="{Binding}">
                                <Button.Style>
                                    <Style TargetType="Button" BasedOn="{StaticResource PillButtonStyle}">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                                <Setter Property="Background" Value="{DynamicResource AccentBrush}" />
                                                <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <TextBlock Style="{StaticResource SidebarLabelStyle}" Text="CATALOG ACTIONS" />
                <Button Content="Remove Selected"
                        AutomationProperties.AutomationId="RemoveSelectedBtn"
                        Style="{StaticResource GhostButtonStyle}"
                        Command="{Binding RemoveSelectedAppCommand}"
                        HorizontalAlignment="Stretch"
                        Margin="0,0,0,5" />
                <Button Content="Reset Defaults"
                        AutomationProperties.AutomationId="ResetDefaultsBtn"
                        Style="{StaticResource GhostButtonStyle}"
                        Command="{Binding ResetDefaultCatalogCommand}"
                        HorizontalAlignment="Stretch"
                        Margin="0,0,0,16" />

                <TextBlock Style="{StaticResource SidebarLabelStyle}" Text="EXECUTION LOG" />
                <Border Background="{DynamicResource InputBgBrush}"
                        BorderBrush="{DynamicResource BorderBrush}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="8,6"
                        MinHeight="180"
                        MaxHeight="360">
                    <TextBox x:Name="LogTextBox"
                             AutomationProperties.AutomationId="LogTextBox"
                             IsReadOnly="True"
                             AcceptsReturn="True"
                             TextWrapping="Wrap"
                             VerticalScrollBarVisibility="Auto"
                             HorizontalScrollBarVisibility="Disabled"
                             FontFamily="{DynamicResource MonoFont}"
                             FontSize="10.5"
                             Text="{Binding LogText}"
                             Background="Transparent"
                             BorderThickness="0"
                             Padding="0"
                             Height="Auto"
                             VerticalAlignment="Top"
                             VerticalContentAlignment="Top" />
                </Border>

                <TextBlock Style="{StaticResource SidebarLabelStyle}" Text="SYSTEM TOOLS" Margin="4,16,0,8" />
                <Button Content="Activate Windows"
                        AutomationProperties.AutomationId="ActivateWindowsBtn"
                        Style="{StaticResource GhostButtonStyle}"
                        Click="ActivateWindows_Click"
                        HorizontalAlignment="Stretch"
                        Margin="0,0,0,16" />

                <CheckBox Content="Silent Install"
                          Style="{StaticResource GlobalCheckBoxStyle}"
                          IsChecked="{Binding IsSilentInstall}"
                          ToolTip="Uncheck this to run installers interactively."
                          Margin="0,0,0,10" />
            </StackPanel>
        </ScrollViewer>

        <Border Grid.Row="1"
                Background="{DynamicResource SurfaceBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="0,1,0,0"
                Padding="14,10">
            <StackPanel>
                <TextBlock FontSize="10"
                           FontWeight="Bold"
                           Foreground="{DynamicResource TextMutedBrush}"
                           Text="STATUS" />
                <TextBlock Margin="0,4,0,0"
                           FontSize="11"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource TextPrimaryBrush}"
                           Text="{Binding StatusText}"
                           TextTrimming="CharacterEllipsis" />
                <TextBlock Margin="0,2,0,0"
                           FontSize="10"
                           Foreground="{DynamicResource TextMutedBrush}"
                           Text="{Binding EtaText}" />
                <ProgressBar Margin="0,6,0,0"
                             Minimum="0"
                             Maximum="{Binding ProgressMaximum}"
                             Value="{Binding ProgressValue}" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>

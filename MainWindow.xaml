<Window x:Class="NewPCSetupWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        xmlns:controls="clr-namespace:NewPCSetupWPF.Controls"
        mc:Ignorable="d"
        Title="Initio"
        Width="1440"
        Height="940"
        MinWidth="1024"
        MinHeight="720"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="CanResize"
        Background="Transparent"
        Icon="pack://application:,,,/Images/app_icon.ico"
        Loaded="Window_Loaded"
        SizeChanged="Window_SizeChanged">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="0"
                            CornerRadius="0"
                            GlassFrameThickness="0"
                            ResizeBorderThickness="8"
                            UseAeroCaptionButtons="False" />
    </shell:WindowChrome.WindowChrome>

    <Border Background="{DynamicResource BackgroundBrush}" CornerRadius="10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="36" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0"
                    Background="{DynamicResource HeaderBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="0,0,0,1"
                    CornerRadius="10,10,0,0"
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="16,0,0,0">
                        <Ellipse Width="7" Height="7" Fill="{DynamicResource AccentBrush}" Margin="0,0,10,0" />
                        <TextBlock FontSize="12"
                                   FontWeight="Bold"
                                   Foreground="{DynamicResource TextPrimaryBrush}"
                                   FontFamily="{DynamicResource AppFont}"
                                   Text="Initio" />
                        <TextBlock Margin="10,0,0,0"
                                   FontSize="11"
                                   Foreground="{DynamicResource TextMutedBrush}"
                                   VerticalAlignment="Center"
                                   Text="New PC Setup" />
                    </StackPanel>

                    <StackPanel Grid.Column="1"
                                Orientation="Horizontal"
                                shell:WindowChrome.IsHitTestVisibleInChrome="True">
                        <Button Style="{StaticResource CaptionButtonStyle}" Content="&#xE921;" Click="WindowMinimize_Click" />
                        <Button Style="{StaticResource CaptionButtonStyle}" Content="&#xE922;" Click="WindowMaxRestore_Click" />
                        <Button Style="{StaticResource CloseCaptionButtonStyle}" Content="&#xE8BB;" Click="WindowClose_Click" />
                    </StackPanel>
                </Grid>
            </Border>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="{Binding SidebarWidth}" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0"
                        Background="{DynamicResource SidebarBrush}"
                        BorderBrush="{DynamicResource BorderBrush}"
                        BorderThickness="0,0,1,0"
                        CornerRadius="0,0,0,10">
                    <controls:SidebarPane ActivateWindowsRequested="SidebarPane_ActivateWindowsRequested" />
                </Border>

                <Grid Grid.Column="1">
                    <Grid.Style>
                        <Style TargetType="Grid">
                            <Setter Property="Margin" Value="16,12,16,12" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsCompactLayout}" Value="True">
                                    <Setter Property="Margin" Value="8" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Grid.Style>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <StackPanel>
                            <TextBlock Style="{StaticResource SectionTitleStyle}" Text="Package Catalog" />
                            <TextBlock Style="{StaticResource SectionDescriptionStyle}"
                                       Text="Select apps to install. Use profiles for quick presets, or browse the store for more." />
                        </StackPanel>

                        <WrapPanel Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Top">
                            <Button Content="Install Selected"
                                    AutomationProperties.AutomationId="InstallBtn"
                                    Command="{Binding InstallSelectedCommand}"
                                    Margin="0,0,5,5" />
                            <Button Content="Cancel"
                                    AutomationProperties.AutomationId="CancelBtn"
                                    Style="{StaticResource DangerButtonStyle}"
                                    Command="{Binding CancelInstallCommand}"
                                    Margin="0,0,0,5" />
                        </WrapPanel>
                    </Grid>

                    <Grid Grid.Row="1" Margin="0,10,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <Button Content="Refresh Installed"
                                AutomationProperties.AutomationId="RefreshInstalledBtn"
                                Style="{StaticResource GhostButtonStyle}"
                                Command="{Binding RefreshInstalledCommand}" />
                        <Border Grid.Column="1"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Center"
                                Background="{DynamicResource SurfaceHighBrush}"
                                CornerRadius="8"
                                Padding="12,5">
                            <TextBlock Text="{Binding SelectionSummary}"
                                       Foreground="{DynamicResource TextSecondaryBrush}"
                                       FontSize="11" />
                        </Border>
                    </Grid>

                    <Border Grid.Row="2" Margin="0,10,0,0" Style="{StaticResource CardStyle}" Padding="0">
                        <TabControl x:Name="MainTabControl"
                                    AutomationProperties.AutomationId="MainTabControl"
                                    Style="{StaticResource TabControlStyle}"
                                    SelectedIndex="0">
                            <TabItem x:Name="CatalogTab" AutomationProperties.AutomationId="CatalogTab" Header="My Setup">
                                <controls:CatalogTabView />
                            </TabItem>
                            <TabItem AutomationProperties.AutomationId="StoreTab" Header="Store">
                                <controls:StoreTabView />
                            </TabItem>
                            <TabItem AutomationProperties.AutomationId="SearchTab" Header="Search">
                                <controls:SearchTabView />
                            </TabItem>
                            <TabItem AutomationProperties.AutomationId="DebloaterTab" Header="Debloater">
                                <controls:DebloatTabView />
                            </TabItem>
                        </TabControl>
                    </Border>
                </Grid>
            </Grid>
        </Grid>
    </Border>
</Window>
